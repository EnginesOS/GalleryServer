<%= content_tag :div, class: 'col-sm-12' do %>

  <%= content_tag :h1 do %>
    <%= content_tag(:i, '', class: 'fa fa-user') %> Edit <%= @user.username %>
  <% end %>

  <%= content_tag :div, class: 'top-gap' do %>

    <%= form_for @user do |f| %>

      <%= render 'shared/form/errors', f: f, label: :Review %>
      <%= render 'shared/form/text_field', f: f, name: :email %>
      <%= render 'shared/form/cancel_and_submit_buttons', f: f %>

    <% end %>

  <% end %>

<% end %>
